{
  "name": "threadbeam",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight, self-hosted dashboard for concurrent AI coding-agent delivery work.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "emit": "node bin/emit.mjs",
    "capture:readme": "node scripts/capture-readme-screenshot.mjs",
    "test": "node --test test/*.node-test.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
